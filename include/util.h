#ifndef __UTIL_H_
#define __UTIL_H_

#define MIN(a, b) ( (a < b) ? a : b )
#define MAX(a, b) ( (a > b) ? a : b )
#define ABS(a) ( ( a ) < 0 ? -1 * ( a ) : a )
#define SQUARE(a) ( ( a ) * ( a ) )
#define SIGN(a) ( ( a ) < 0 ? -1 : 1 )

#endif // __UTIL_H_
